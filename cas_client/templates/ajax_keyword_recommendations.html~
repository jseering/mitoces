{% if keywords.count > 0 %}
    {% for keyword in keywords %}
	        <button type="button" value="{{ keyword.id }}" class="keyword_search_result_button" title="Click to remove">{{ keyword }}</button>
    {% endfor %}
{% endif %}
