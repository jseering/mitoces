{% if keywords.count > 0 %}
    {% for keyword in keywords %}
	        <button type="button" value="{{ keyword.id }}" id="add_keyword_{{ keyword.id }}">{{ keyword }}</button>
    {% endfor %}
{% endif %}
